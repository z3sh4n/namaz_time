import 'package:flutter/material.dart';

class QuranSharifScreen extends StatelessWidget {
  const QuranSharifScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}